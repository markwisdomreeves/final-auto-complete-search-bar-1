import React, { Component } from "react";

export default class SubCategeryTitle extends Component {
    render() {
        const title = "Seleziona una sotto-categoria";
        return <h2 className="sub-category-title">{title}</h2>
    }
}
